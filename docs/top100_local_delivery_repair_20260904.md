# 2026-09-04 主目录同步与防复发修复

## 已完成的同步

- 主目录从 `d0ebe889e56419ed87ff729a63ea292e87a2655e` 快进到已部署策略提交 `96e7183c90a6f6aa22f54477931638c41a683bb4`。
- 原未提交文件先备份到 `.codex_backups/20260904_151859`；Git stash `codex-preserve-local-before-sync-20260904` 继续保留。
- 合并两个冲突文件时同时保留远端缓存包完整性检查与今天本地新增的实时 preflight。没有重置工作区或丢弃未提交修改。
- 冻结基准同步为 `2026-09-03-post-rebalance-bootstrap-v3`，seed_end_date 为 2026-09-03。
- 通过 `restore_approved_top100_seed.py` 核验生产工作树与本地主目录：三版源码（仅规范化 CRLF/LF）相同，六项底层文件均匹配已批准 authority 的精确哈希，然后成套恢复 24 份三版本产物。新旧哈希记录在备份目录 `approved_seed_restore.json`。
- 这是已批准生产种子的本地同步，不是批准新历史、修改 ST 政策或放宽历史改写检查。恢复后依次通过三个正式入口、不带迁移参数重跑；三版均为 clean。
- 三版成本净值截至 2026-09-03，v2.0 4015 行、v2.3/v2.5 各 3970 行。面板 8720 行、代理指数 4049 行、基础成本净值 4032 行、换手 431 条调仓记录，均截至相应的 2026-09-03 日/事件。

## 防复发机制

1. `top100_delivery.py refresh-all` 串行刷新底层和三个正式入口，只在全部成功后原子写 complete 清单。历史改写仍按原正式规则拒绝。
2. `check` 独立检查实时远端 main 的核心源码/authority、官方指数历史确定的最新确认日，再读回三个版本全部核心产物。不是读取 stdout 的成功标志。
3. 清单记录输入、源码、冻结基准、最终文件的内容哈希。只改内容不改日期也会失效；同日旧产物不能蒙混过关。
4. 生成期间共享输入变化、跨越新收盘确认时点、子进程失败、缺文件、重复/倒序日期、版本身份不符、NAV 别名不一致、未 clean 的迁移审计均阻断。
5. 整组锁防止并发刷新；异常保留 blocked 清单，进程强杀遗留锁必须经人工检查，不能自动清除正在运行的锁。
6. 原 validate 明示 `scope=base_state_only`、`final_outputs_validated=false`；README 与 AGENTS 改用整组入口作为同步验收。

## 验证及边界

- 新增缺失清单、任一版本旧产物、同日内容变更、身份错误、失败子进程、输入并发变化、传输换行、时间推进与并发锁回归。
- 全仓 `python -X utf8 -m pytest -q`：243 passed，2 个警告来自故意构造重复 ZIP 项的防篡改测试。
- 未修改策略参数、费用模型、实盘下单或日报发送配置。无重发邮件。
- 日报已部署的 96e7183c 与本地三版核心源码一致；在线整组验收以 `outputs/top100_delivery_manifest.json` 实际状态及随后 `check` 退出码为准，不能把本记录的 9 月 3 日同步结果当成 9 月 4 日新鲜度证明。
- 本次真实 `refresh-all` 于北京时间 15:32:32 写入 `status=complete`、`ok=true`、`scope=whole_workspace_delivery`，随后单独运行 `check` 退出 0。两次独立官方历史加载器返回的最新确认日均为 2026-09-03；没有声称已生成 2026-09-04 收盘数据。初次接口超时按预期返回非零，重试成功后才写 complete。
- 新增整组工具和本地 preflight 保护属于本地主目录修复，本轮未推送或更改云端邮件流水线。云端仍使用已成功投递的生产提交 96e7183c；不能把本地新工具称为已在云端部署。
- 既有 ST 元数据历史政策标签完整性缺口属于独立数据质量问题，本次没有补写标签、批准新的元数据哈希或声称该问题已修复；见研究目录中的 lineage_audit.md。

## 恢复

旧源码、基准、三版产物及实时缓存保留在上述备份；Git stash 未删除。不要单独恢复一份旧 NAV，回滚必须按同一组源码、输入、authority 和最终产物进行，再通过整组验收。
