# 2026-09-05 日报备用行情发布验收

## 发布范围

中证1000历史行情的备用链同时接入本地状态预检与 GitHub 正式历史刷新。不是个股实时快照、ST元数据或交易日历的通用备用链；这些数据仍执行原独立校验。

- 正式历史刷新：东方财富 → 新浪旧历史 → 新浪静态历史 → 腾讯历史。
- 本地 state-only 预检：新浪静态历史 → 腾讯历史，保留 60 秒进程硬超时。
- 逐源检查完整交易日集合、日期新鲜度、原始指数身份、OHLC、有限正数、成交量及重复记录。主源连接失败、超时或数据无效时自动尝试下一源。
- 所有主/备源切换都不能改写已有价格：0.01 指数点以内显示精度差异保留原值；实质冲突直接阻断。所有源均失败仍 BLOCKED，不把旧文件或残缺数据冒充当日数据。
- 不改 v2.0/v2.3/v2.5 策略参数、费用和执行假设，不下单。本文不发布新回测绩效。

策略发布：PR #49 增加备用链，PR #50 补强所有源切换的价格一致性，最终合并 SHA `a6820443360ee24ee109ebeb9d0a31485237ae0c`。

自动化发布：PR #60 / #61，最终合并 SHA `9c56df2123c595935ce90e39527b42f77654231e`，3 个策略 checkout 均固定到上述同一策略 SHA。18:00 调度、收件人、发送防重与最终 CSV 身份门禁不变。

本地任务 `ic-im` 已通过 Codex 自动化工具更新并读回：ACTIVE、原每日 14:30 调度、模型、项目及 IC/IM 原有规则均保留；新增备用链说明与专项测试。不在周末补发盘中日报。

手工周末纠正版的整包目标日改为独立交易日历所得最近已完成交易日，而非周末自然日；定时/外部调度仍保留当天信号要求和休市门禁。

## 本地实际验收

- 全仓：511 passed，2 个已有 ZIP 篡改测试 warning。
- 干净发布目录：504 passed，1 个数据依赖 skip，2 个已有 warning。
- 自动化：253 passed，90 subtests passed；固定引用更新后专项16项通过；PR 云端三组回归全通过。
- 本地 14:30 全部离线专项：289 passed，13.69 秒，60 秒硬超时仍有效。
- 最终正式 `scripts/top100_delivery.py refresh-all`：128.044 秒、退出0。
- 随后的独立 `scripts/top100_delivery.py check`：9.291 秒、退出0。
- 三版 costed NAV 的日期、持仓、下一持仓、每日收益、净值、费用一致，文件 SHA-256 前后相同，最大净值差 0。
- 底层成本表及三版净值中的每行指数价格，均与自身面板完全一致，最大差0。

真实网络故障并非只有模拟：上述正式整组刷新日志两次记录东方财富断连、新浪旧接口超时，然后新浪静态历史自动接管成功。没有手动改源或绕过门禁。

另用正式历史入口进行故障注入：仅注入前序接口超时，后续行情仍是真实联网数据。新浪静态和腾讯均得到 2026-02-06 至 2026-09-04 共140个交易日；新浪与既有历史价格差0，腾讯原始显示精度差最大0.005点，经保留原值后无历史价格变更。注入期间正式输入和NAV文件均未修改。

## 新鲜度读回

| 流 | 行数 | 末日 |
|---|---:|---|
| panel | 8721 | 2026-09-04 |
| proxy index | 4050 | 2026-09-04 |
| base costed NAV | 4033 | 2026-09-04 |
| v2.0 NAV | 4016 | 2026-09-04 |
| v2.3 NAV | 3971 | 2026-09-04 |
| v2.5 NAV | 3971 | 2026-09-04 |
| turnover调仓事件表（非日流） | 431 | 2026-09-03 |

最新已完成交易日经独立日历确认为09-04。没有填造周末价格或09-04调仓事件。三版最终信号均1行、日期09-04。

## 云端交付验收

实际最终工作流：`33895015058`，以 `close_confirmed`、明确纠正版运行；不是18:00定时触发或盘中实盘验收。

状态：**最终工作流 completed/success，整包、最终CSV、内部价格一致性和Gmail收件全部通过。**

- 从最终工件独立读回：v2.0/v2.3/v2.5 日期均09-04，revision分别为已批准朴素v2.0、26%/20%且R²关闭的朴素v2.3、原v2.5；成员动作均False，历史09-03信号/09-04执行日期未渲染为当前交易。
- `cloud_acceptance.json`：策略SHA与自动化SHA匹配，所有基础输入哈希均与本地一致（`changed_inputs=[]`），三版每日收益、净值、对冲价格、持仓及费用差异均0。
- `cloud_price_coherence.json`：基础costed表4033行、v2.0的4016行、v2.3/v2.5各3971行，逐行指数价格均与面板一致，最大差0；同时检查有限数。
- 云端本次日志14次记录东方财富连接失败、自动切到新浪旧历史成功；新增新浪静态/腾讯分别已有本地真实网络与逐级故障注入验收，不冒称本轮云端实际使用了这两个更后序接口。
- Gmail实际收件：2026-09-05 **00:31:10 北京时间**，message id `1a06d4283d0752e3`。已完整读取正文，核对最终策略SHA与run链接，主题明确为纠正版/收盘确认，信号日09-04。工作流的发送和完成marker都成功。
- 最终工件目录为 `cloud_final_bundle/` 与 `cloud_final_digest/`，机器可读收件证据为 `email_receipt.json`。此次是手工收盘确认纠正版验收，不代表已经观察到未来14:30/18:00定时触发；未来全源失效仍应BLOCKED。

第一轮 `33893690575` 确实工作流成功且 Gmail 收到（message id `1a06d34efef91a7c`），三版最终净值当时与本地一致。但额外检查发现底层成本表最后一行用了东方财富7508.09，最终面板随后采用新浪7508.092，存在0.002点内部价格差异。独立逐行检查准确拒绝第一轮工件；因此追加PR #50/#61，将保留历史价格的规则扩展到所有主/备源切换，并重新运行，不把第一轮作为最终验收。第一轮证据保留在 `attempt1/` 和原始 `cloud_bundle/` / `cloud_digest/`。

## 证据与回滚

证据目录：`research_reports/20260905_index_source_deployment/`，包括 `live_failover.json`、`local_refresh.json`、整组刷新/校验日志及两组本地 JUnit XML。

备份：主工作区代码 `.codex_backups/20260905_000108/` 与价格补丁前 `.codex_backups/20260905_002019/`，数据 `.codex_backups/20260905_000901/`；本地自动化 `D:/Codex/home/automations/ic-im/.codex_backups/20260905_000728/`；发布工作区备份分别保留在对应 `.codex_backups/`。只发布此次 reviewed delta，没有重置主工作区或覆盖其他研究改动，没有删除数据。

回滚应评审并反向应用上述PR的差量，同时保留后来改动；本地自动化必须通过工具恢复相应字段，不直接覆写整份配置。不得手改净值或放宽历史改写审计来回滚。
