# 日报指数行情备用源

本地 14:30 预检与 GitHub 18:00 历史刷新共用 `scripts/index_history_preflight.py` 的严格校验。

- 正式历史刷新：东方财富 → 新浪旧历史 → 新浪静态历史 → 腾讯历史。连接错误、超时、数据过期或校验失败均切换下一源。
- 只读状态预检：新浪静态历史 → 腾讯历史，避免在 60 秒预算中重复消耗已知慢接口的多轮重试。
- 每个 HTTP 请求 timeout=8 秒；所有源都失败则 BLOCKED，不使用陈旧缓存冒充更新。外层保留进程硬超时。
- 只接受中证1000原始指数日行情；按独立交易日历校验完整日期集合、OHLC、有限正价格、非负成交量。拒绝拼接缺日、调整股价替代指数、非交易日和重复行。
- 新浪远端编码仅作为数据交给本地已安装解码器，不执行远端 JavaScript。
- 备用源与已存历史差异超过 0.01 指数点即阻断；允许的显示精度差异保留既有价格，不改写历史。新交易日仍必须有真实完整行情。
- 源选择、失败尝试和日期写入预检证明；历史刷新日志包含 `[index-history]` 来源与重合期精度检查，随云端工件保留。
- 保留当天快照、100/100、ST、成员、历史改写与最终 CSV 身份校验。备用历史源不等于个股/实时快照/交易日历也拥有相同备用链。

执行 `python -X utf8 -m pytest tests/test_index_history_preflight.py` 验证逐源故障回退、全源失败关闭及历史价格不变。没有策略参数、成本或交易执行变更。
