# POE Bots

- `legacy/` 保存历史版本，避免和当前正式入口混淆。
- `poe_bots/` 根目录下的正式入口文件现在都是独立单文件版本，不依赖主脚本或本地 CSV。
- 正式入口会自行在线拉取指数、全市场快照和个股历史，支持 `信号`、`实时信号`、`成分股`、`进出名单`、`实时进出名单`、`表现`。
- 默认版本是 `1.1` 主线版；`1.2` 是备选防守版；`1.0` 是历史基线版。
- 命令输出统一写入 `outputs/poe_command_outputs/`。
