# POE Bots

- `legacy/` 保存历史版本，避免和当前正式入口混淆。
- 顶层只保留一个正式入口: `poe_bots/microcap_top100_poe_bot.py`。
- 正式入口是独立单文件版本，不依赖主脚本或本地 CSV。
- 入口会自行在线拉取指数、全市场快照和个股历史，支持 `信号`、`实时信号`、`成分股`、`进出名单`、`实时进出名单`、`表现`。
- 默认版本是 `1.1` 主线版；`1.2` 是备选防守版；`1.0` 是历史基线版。
- 命令输出统一写入 `outputs/poe_command_outputs/`。
