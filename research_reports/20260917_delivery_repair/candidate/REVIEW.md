# 微盘股日报历史状态纠偏候选审阅

**这是尚未批准的诊断候选，不是正式信号或生产历史。`approved=false`；尚未部署、发信或迁移。**

## 已验证范围

- 使用 9 月 16 日云端正式 whole 包作为原始基准，先通过三版本纯函数回放校验：日期索引一致，持仓、下一持仓、成本、收益及净值等重要字段零差异（数值容差 1e-10 相对 / 1e-12 绝对）。
- 使用已经批准、SHA-256 为 `9558082806163587d9813100bd19cafe92337a2c086e3fc8ac6aa8a71e5825ee` 的 9 月 3 日执行后 100 只成员作为续算种子。
- proxy 截至 9 月 3 日的原有字段值精确不变；三版本截至该日的全部原有共同字段均通过冻结比较。日期空字符串与 CSV 空值按同为空日期处理；该处理不改变持仓或数值。
- 从 9 月 4 日起按真实行情缓存及正式交易约束函数续算至 9 月 17 日。9 月 17 日目标名单取自主工作区正式 proxy 目标文件，其精确哈希已记录，不能与另一云端目标混用。
- 修订范围包含 9 月 4 日至 9 月 16 日原有行及新增 9 月 17 日行。每日 proxy 差异见 `candidate_proxy_daily_differences.csv`；三版本逐字段差异见 `candidate_cell_differences.csv`。

## 三版本候选

| 版本 | 行数 | 末日 | SHA-256 |
|---|---:|---|---|
| v2.0 | 4025 | 2026-09-17 | `adb94b002a9eaeb235dddd6b9f9209307eeb397f0cbd247d4b35e0761558ddf0` |
| v2.3 | 3980 | 2026-09-17 | `a2060e533d674d75721026ba6840324cf82ec4dd625f4b0208153ecff89cd5a7` |
| v2.5 | 3980 | 2026-09-17 | `fa0818a3bddebcd19b8d5469930cb93cf68fa0af63c39fdd9c882fe7615d400f` |

## 待批准动作

批准对象应限定为本目录精确哈希候选所示的历史 lineage 纠偏及对应三版本重算。当前报告只供审阅；不含任何已批准标志，也不替代程序要求的历史迁移授权文件。若输入、目标名单、代码或候选哈希变化，需重新生成和审阅。

批准后仍须在正式迁移流程下重建并读回三版本，再无迁移开关运行一次通过干净 rewrite audit；最后完成全组 refresh-all/check 及云端日报实际消费验收。测试通过或本候选生成成功均不等于两条日报已上线恢复。

## 复现与证据

- `build_diagnostic_candidate.py`：只写本诊断目录，使用原始云包、真实缓存和隔离工作区修复函数。
- `exact_hash_migration_review.json`：输入源码、云包、行情/股本/元数据及候选产物的精确 SHA-256。
- `candidate_proxy.csv`、`candidate_turnover.csv`、`candidate_effective_members.csv`：供审阅的基础状态候选。
- 早先全字段比较发现的空日期序列化假差已经排除；最终状态以当前 JSON 为准。
